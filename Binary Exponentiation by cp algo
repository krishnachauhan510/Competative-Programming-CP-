long long binpow(long long a, long long b) {
    if (b == 0)
        return 1;
    long long res = binpow(a, b / 2);
//for integer calculation and modulo operations   binpow is better than pow
    if (b % 2)
        return res * res * a;
    else
        return res * res;
}
